{
  "config": {
    "step": {
      "user": {
        "title": "Set up PointsBot",
        "description": "PointsBot will automatically create a points profile for every Home Assistant person entity. No additional configuration is required."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single PointsBot integration instance is allowed."
    },
    "error": {}
  }
}
